export enum QueryKeys {
  INVENTORY = 'inventory',
}

export enum EditActions {
  ADD = 'add',
  REMOVE = 'remove',
}

export enum RoomClasses {
  STANDARD = 'Standard',
  DELUXE = 'Deluxe',
  SUITE = 'Suite',
}
